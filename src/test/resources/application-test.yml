spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:url_shortener_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  h2:
    console:
      enabled: false

  sql:
    init:
      mode: never

logging:
  level:
    root: WARN
